// examples/demo.ltl — runnable demo for ltl_scheme_lite

import std::io
use ltl_scheme_lite as lib

fn main() {
    io::println("ltl_scheme_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}